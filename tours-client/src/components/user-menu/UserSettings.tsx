import React, { FC } from 'react';
import { Box } from '@chakra-ui/react';

const UserSettings: FC = () => {
  return (
    <Box h='70vh' className='random'>
      User Settings Page
    </Box>
  );
};

export default UserSettings;
